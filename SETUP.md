
# SETUP (v2.5)

## Pré-requisitos
- Node 18 (use `.nvmrc`)
- npm

## Instalação
```
cd server
npm install
npm start
```

## Docker (opcional)
```
docker compose up -d --build
```

## Variáveis de ambiente
Veja `ENV.example`.
